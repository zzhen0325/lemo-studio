[project]
requires = ["nodejs-24"]

[dev]
build = ["pnpm", "install"]
run = ["bash", "-c", "APP_PORT=5000 pnpm run dev"]

[deploy]
build = ["./scripts/deploy-build.sh"]
# 运行启动脚本（脚本内部会切换目录并设置环境变量）
run = ["sh", ".next/standalone/start.sh"]
