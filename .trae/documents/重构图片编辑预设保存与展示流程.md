# 图片编辑预设功能重构计划

## 1. 数据模型升级
- 修改 `Preset` 类型，通过 `editConfig` 的存在与否自动识别预设类型。
- 确保 `EditPresetConfig` 能够承载：
  - Fabric.js 画布完整状态 (JSON)
  - 原始图片 URL
  - 参考图片数组 (ID, DataURL, Label)
  - 标注信息数组 (颜色, 文本, 关联图)
  - 画布元数据 (尺寸, 背景色)

## 2. 预设管理器 (PresetManagerDialog) 深度定制
- **动态表单**：
  - 如果是 **编辑预设**：隐藏 Prompt 文本域和模型参数配置。
  - 新增 **编辑状态摘要区域**：展示原始图预览、参考图缩略图列表、标注条数统计。
- **校验逻辑**：
  - 放宽保存限制：编辑预设不需要必填 Prompt，仅需名称和有效的画布数据。
- **交互优化**：
  - 在保存编辑预设时，默认使用编辑器当前的画面作为预设封面图。

## 3. 图片编辑器 (ImageEditorModal) 联动
- 完善 `handleSavePreset` 函数，确保捕获当前画布的所有图层（包括文字、形状、图片）。
- 自动填充预设名称（如“未命名编辑预设”）。

## 4. 列表与加载逻辑
- **类型标识**：在预设列表卡片上增加“编辑”标识。
- **一键恢复**：点击编辑预设时，直接打开编辑器并恢复所有图层状态，而非仅填入 Prompt。

请确认以上方案，确认后我将开始代码实现。