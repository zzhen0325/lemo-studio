## 修复 Playground 编辑模式下下拉框不可见的问题

### 1. 调整 ControlToolbar.tsx 逻辑

* <br />

* **提升下拉框层级**：为模型选择和分辨率选择的 `DropdownMenuContent` 增加 `z-[10001]` 样式，确保其在编辑器模态框（`z-[9999]`）之上显示。

* <br />

###

### 3. 验证修改

* 进入图片编辑模式，点击各个下拉框和按钮，确保菜单和弹窗均能正常显示在编辑器最前方。

