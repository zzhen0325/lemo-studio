## 目标调整
- 仅新增 GenerationConfig.workflowName，用于记录 UI 的工作流显示名称。
- 保持现有字段：model（实际模型/检查点）、lora（串值）不变；仅调整写入来源为 ControlToolbar 的选择值。

## 类型变更
- 在 /types/database.ts 的 GenerationConfig 中新增：
  - workflowName?: string

## 写入与来源
- 在生成服务：
  - Workflow 场景：
    - config.model 继续写入解析出的实际模型（如 flux1-dev-fp8.safetensors）
    - config.lora 保持现状（串值）
    - 新增写入 config.workflowName = ControlToolbar.selectedWorkflowName
  - 普通模型场景：
    - 不影响现有行为，可不写 workflowName 或置为 undefined
- 保存图片的 metadata.config 含上述字段；历史 API 透传

## 展示
- Gallery/History 展示时，如存在 workflowName，显示该名称用于标识来源；否则回退现状

## 兼容
- 旧数据不包含 workflowName 前端自动回退，不影响功能

## 实施步骤
1) 修改 /types/database.ts，新增 workflowName 字段
2) 在 useGenerationService 工作流分支写入 workflowName（读取 ControlToolbar 的 selectedWorkflowName）同时保持 model/lora 写入策略
3) 验证 outputs/*.json 与历史展示

确认后我将按此方案修改类型与生成服务，完成验证。