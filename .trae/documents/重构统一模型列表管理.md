# 统一模型列表重构计划

## 任务目标
1. 在 [useGenerationService.ts](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/hooks/features/PlaygroundV2/useGenerationService.ts) 中定义并导出统一的模型配置列表 `AVAILABLE_MODELS`。
2. 重构 [useGenerationService.ts](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/hooks/features/PlaygroundV2/useGenerationService.ts) 以使用该统一列表进行模型 ID 映射。
3. 重构 [ControlToolbar.tsx](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/components/features/playground-v2/ControlToolbar.tsx)，使用统一列表并在下拉菜单中仅显示 **Nano banana** 和 **Coze Seed 4**。
4. 重构 [PresetManagerDialog.tsx](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/components/features/playground-v2/Dialogs/PresetManagerDialog.tsx)，使用统一列表动态生成模型选项。

## 详细实施步骤

### 1. 修改 [useGenerationService.ts](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/hooks/features/PlaygroundV2/useGenerationService.ts)
- 定义 `UnifiedModelConfig` 接口。
- 定义并导出 `AVAILABLE_MODELS` 常量数组，包含所有模型的 `id`, `displayName`, `modelKey` 和 `backendModelId`。
- 更新 `handleUnifiedImageGen` 逻辑，通过查找 `AVAILABLE_MODELS` 来获取 `backendModelId`。

### 2. 修改 [ControlToolbar.tsx](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/components/features/playground-v2/ControlToolbar.tsx)
- 移除本地的 `MODEL_CONFIG`。
- 导入 `AVAILABLE_MODELS`。
- 更新 `triggerLabel` 和映射逻辑以使用统一配置。
- 在 `ModelDropdown` 中过滤 `AVAILABLE_MODELS`，仅保留 `nano_banana` 和 `coze_seed4` 进行显示。

### 3. 修改 [PresetManagerDialog.tsx](file:///Users/bytedance/Desktop/seeseezz/lemoai_gulux/Lemon8_ai_studio/components/features/playground-v2/Dialogs/PresetManagerDialog.tsx)
- 移除本地的 `NATIVE_MODELS` 数组。
- 导入 `AVAILABLE_MODELS`。
- 动态生成原生模型判断逻辑。
- 将 `Base Model` 的 `SelectItem` 改为基于 `AVAILABLE_MODELS` 动态渲染。

请确认此计划是否符合您的需求。确认后我将开始执行修改。
